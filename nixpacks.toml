[phases.setup]
nixPkgs = ["...", "php82", "php82Extensions.pdo", "php82Extensions.pdo_mysql", "php82Extensions.mbstring", "php82Extensions.xml", "php82Extensions.curl", "php82Extensions.zip", "php82Extensions.gd", "php82Extensions.bcmath", "php82Extensions.intl", "php82Extensions.opcache", "php82Extensions.tokenizer", "php82Extensions.fileinfo"]

[phases.install]
cmds = ["composer install --optimize-autoloader --no-dev", "npm install && npm run build"]

[start]
cmd = "php artisan key:generate --force && php artisan migrate --force && php artisan config:cache && php artisan route:cache && php artisan view:cache && php artisan serve --host=0.0.0.0 --port=${PORT:-8080}"
